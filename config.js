// Central backend endpoint configuration.
// Loaded as a classic script BEFORE any other app script, so these
// top-level constants are available globally to every other file.
const API_BASE_URL = "https://api.itarla.com";
const AUTH_BASE_URL = "https://auth.itarla.com";
